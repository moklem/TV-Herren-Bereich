---
phase: 01-backend-stability
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - server/models/Event.js
autonomous: true
requirements:
  - PERF-06

must_haves:
  truths:
    - "Event model defines compound indexes for votingDeadline+autoDeclineProcessed and endTime+attendanceAutoProcessed"
    - "Event model defines a single-field index on startTime"
    - "MongoDB applies the three indexes on next Mongoose connection (autoIndex: true default)"
  artifacts:
    - path: "server/models/Event.js"
      provides: "Three new index declarations on EventSchema"
      contains: "EventSchema.index({ startTime: 1 })"
  key_links:
    - from: "server/models/Event.js"
      to: "server/utils/votingDeadlineJob.js"
      via: "compound index supports { votingDeadline, autoDeclineProcessed } query"
      pattern: "votingDeadline.*autoDeclineProcessed"
    - from: "server/models/Event.js"
      to: "server/routes/eventRoutes.js"
      via: "startTime index supports sort in GET / list endpoint"
      pattern: "sort.*startTime"
---

<objective>
Add three Mongoose compound indexes to the Event model to support the recurring background job queries and the event list sort.

Purpose: votingDeadlineJob and attendanceTrackingJob run every minute and do full collection scans without indexes. Adding targeted compound indexes eliminates those scans.
Output: Three EventSchema.index() declarations in server/models/Event.js.
</objective>

<execution_context>
@C:/Users/morit/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/morit/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-backend-stability/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add compound indexes to Event model</name>
  <files>server/models/Event.js</files>
  <action>
    1. Open server/models/Event.js. Find the line `module.exports = mongoose.model('Event', EventSchema);` (it will be near the end of the file).

    2. Directly BEFORE that `module.exports` line, insert the following three index declarations:
       ```javascript
       // Indexes for background job queries and event list sort
       EventSchema.index({ startTime: 1 });
       EventSchema.index({ votingDeadline: 1, autoDeclineProcessed: 1 });
       EventSchema.index({ endTime: 1, attendanceAutoProcessed: 1 });
       ```

    3. Do not modify anything else in the file — do not touch the schema fields, virtuals, methods, or any other part of the model.

    Context: These indexes support:
    - `startTime` — used by GET /api/events sort({ startTime: 1 })
    - `{ votingDeadline, autoDeclineProcessed }` — used by votingDeadlineJob.js query
    - `{ endTime, attendanceAutoProcessed }` — used by attendanceTrackingJob.js query

    Note: The Event collection is small (under 1000 documents per team). Index creation on startup will be near-instantaneous and will not block requests.
  </action>
  <verify>
    <automated>node -e "const s = require('fs').readFileSync('server/models/Event.js','utf8'); const hasStartTime = s.includes(\"EventSchema.index({ startTime: 1 })\"); const hasVoting = s.includes('votingDeadline') && s.includes('autoDeclineProcessed'); const hasAttendance = s.includes('endTime') && s.includes('attendanceAutoProcessed'); console.log('startTime:', hasStartTime, 'voting:', hasVoting, 'attendance:', hasAttendance); process.exit(hasStartTime && hasVoting && hasAttendance ? 0 : 1);"</automated>
  </verify>
  <done>Event.js has three EventSchema.index() calls before module.exports. No other changes to the file.</done>
</task>

</tasks>

<verification>
After task completes:
1. `grep "EventSchema.index" server/models/Event.js` returns exactly 3 lines
2. All three indexes are placed before `module.exports`
3. `grep "module.exports" server/models/Event.js` still returns the same model export line
</verification>

<success_criteria>
- server/models/Event.js contains exactly three EventSchema.index() declarations
- Indexes cover: { startTime: 1 }, { votingDeadline: 1, autoDeclineProcessed: 1 }, { endTime: 1, attendanceAutoProcessed: 1 }
- No other changes to the Event model
</success_criteria>

<output>
After completion, create `.planning/phases/01-backend-stability/01-02-SUMMARY.md`
</output>

---
phase: 02-pdf-import-mobile-layout
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - client/src/pages/coach/Events.js
autonomous: true
requirements:
  - LAYOUT-01

must_haves:
  truths:
    - "Coach event page root container has pb: 10 so event cards are not hidden behind BottomNavigation on mobile"
    - "Header row with 'Spielplan importieren' and 'Neuer Termin' buttons wraps on narrow screens — no horizontal overflow"
    - "Both buttons remain fully functional (same RouterLink targets, same icons, same labels)"
  artifacts:
    - path: "client/src/pages/coach/Events.js"
      provides: "Mobile-corrected event list page"
      contains: "pb: 10"
  key_links:
    - from: "client/src/pages/coach/Events.js"
      to: "client/src/components/layout/CoachLayout.js"
      via: "BottomNavigation clearance via pb: 10 on root Box"
      pattern: "pb:\\s*10"
---

<objective>
Fix the coach event page mobile layout so it renders correctly on small screens without bottom navigation overlap or header button overflow.

Purpose: LAYOUT-01 — coaches must be able to see and use all event page controls on mobile. Two layout bugs exist: missing bottom padding causes event cards to hide behind the fixed BottomNavigation bar; two header buttons overflow on screens narrower than ~380px.

Output: Modified `Events.js` with `pb: 10` on root container and a responsive header button row.
</objective>

<execution_context>
@C:/Users/morit/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/morit/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-pdf-import-mobile-layout/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Events.js root container bottom padding and responsive header buttons</name>
  <files>client/src/pages/coach/Events.js</files>
  <action>
Two targeted edits to `client/src/pages/coach/Events.js`:

**Edit 1 — Add `pb: 10` to root Box (line ~262):**

Current:
```javascript
<Box sx={{ mt: 2 }}>
```
Change to:
```javascript
<Box sx={{ mt: 2, pb: 10 }}>
```
This is the mandatory project convention from CLAUDE.md: all coach page root containers must include `pb: 10` (80px) to clear the fixed BottomNavigation bar on mobile.

**Edit 2 — Make header button row responsive (lines ~263-288):**

Current outer Box:
```javascript
<Box sx={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center', mb: 3 }}>
  <Typography variant="h4" component="h1">
    Termine
  </Typography>
  <Box sx={{ display: 'flex', gap: 2 }}>
    <Button variant="outlined" color="primary" startIcon={<UploadFile />} component={RouterLink} to="/coach/events/import-pdf">
      Spielplan importieren
    </Button>
    <Button variant="contained" color="primary" startIcon={<Add />} component={RouterLink} to="/coach/events/create">
      Neuer Termin
    </Button>
  </Box>
</Box>
```

Change to:
```javascript
<Box sx={{
  display: 'flex',
  justifyContent: 'space-between',
  alignItems: { xs: 'flex-start', sm: 'center' },
  flexDirection: { xs: 'column', sm: 'row' },
  gap: 1,
  mb: 3
}}>
  <Typography variant="h4" component="h1">
    Termine
  </Typography>
  <Box sx={{ display: 'flex', gap: 1, flexWrap: 'wrap' }}>
    <Button
      variant="outlined"
      color="primary"
      startIcon={<UploadFile />}
      component={RouterLink}
      to="/coach/events/import-pdf"
    >
      Spielplan importieren
    </Button>
    <Button
      variant="contained"
      color="primary"
      startIcon={<Add />}
      component={RouterLink}
      to="/coach/events/create"
    >
      Neuer Termin
    </Button>
  </Box>
</Box>
```

The `flexDirection: { xs: 'column', sm: 'row' }` stacks title above buttons on narrow screens. `flexWrap: 'wrap'` on the button Box ensures buttons wrap if needed. `gap: 1` (8px) replaces `gap: 2` (16px) between buttons to save space.

Do NOT change button labels, icons, link targets, or any other part of the file.
  </action>
  <verify>
    <automated>grep -n "pb: 10" client/src/pages/coach/Events.js && grep -n "flexDirection" client/src/pages/coach/Events.js && grep -n "flexWrap" client/src/pages/coach/Events.js</automated>
    <manual>Check that the file compiles without errors: cd client && npx react-scripts build --profile 2>&1 | grep -E "ERROR|error" | head -20</manual>
  </verify>
  <done>
    - `client/src/pages/coach/Events.js` root Box includes `pb: 10`
    - Header outer Box has `flexDirection: { xs: 'column', sm: 'row' }` and `alignItems: { xs: 'flex-start', sm: 'center' }`
    - Button container has `flexWrap: 'wrap'`
    - Both buttons unchanged in behavior (same links, labels, icons)
  </done>
</task>

</tasks>

<verification>
After execution:
1. `grep -n "pb: 10" client/src/pages/coach/Events.js` — must return a match on the root Box line
2. `grep -n "flexDirection" client/src/pages/coach/Events.js` — must show `{ xs: 'column', sm: 'row' }`
3. `grep -n "import-pdf" client/src/pages/coach/Events.js` — must still exist (button link preserved)
</verification>

<success_criteria>
- Root `<Box>` has `sx={{ mt: 2, pb: 10 }}` — event cards are not clipped by BottomNavigation on mobile
- Header buttons stack vertically on xs screens and display horizontally on sm+ screens
- No layout regressions on desktop (sm+ behavior unchanged)
</success_criteria>

<output>
After completion, create `.planning/phases/02-pdf-import-mobile-layout/02-01-SUMMARY.md` with:
- Files modified
- Exact changes made
- Verification results
</output>

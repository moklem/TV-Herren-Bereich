---
phase: 02-pdf-import-mobile-layout
plan: 03
type: execute
wave: 2
depends_on:
  - 02-01
  - 02-02
files_modified: []
autonomous: false
requirements:
  - LAYOUT-01

must_haves:
  truths:
    - "Coach event page renders correctly on mobile — no navigation overlap, no button overflow"
    - "PDF import flow shows per-match checkboxes and duplicate detection dialog"
  artifacts: []
  key_links: []
---

<objective>
Human verification checkpoint: confirm the mobile layout and PDF import flow work correctly on a real device or browser mobile emulation before the phase is declared complete.

Purpose: Both plans 02-01 and 02-02 are automated code edits. Visual mobile rendering and interactive UI flows require human confirmation — this is the single verification gate for the phase.

Output: Human approval (or issue report) before moving to Phase 3.
</objective>

<execution_context>
@C:/Users/morit/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/morit/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-pdf-import-mobile-layout/02-01-SUMMARY.md
@.planning/phases/02-pdf-import-mobile-layout/02-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Verify: Mobile layout and PDF import flow on device/emulation</name>
  <action>
Human verification required. Claude has completed all automated changes in plans 02-01 and 02-02. This checkpoint confirms the changes work correctly in the browser before the phase is closed.
  </action>
  <what-built>
    Plan 02-01: Events.js root Box now has pb: 10; header button row is responsive (stacks vertically on mobile, side-by-side on desktop).
    Plan 02-02: ImportMatchesPDF.js step 2 table has per-match checkboxes; handleCreateEvents detects duplicates and shows a dialog; debug console.logs removed from client and server.
  </what-built>
  <how-to-verify>
**Preparation:**
Start the development server or open the test environment (https://inteam-test.onrender.com) and open DevTools in Chrome/Edge.

**Test 1 — Events page mobile layout:**
1. Navigate to /coach/events
2. Open DevTools → Toggle Device Toolbar → Select "iPhone SE" (375x667) or similar narrow viewport
3. Verify: The "Termine" title and both buttons are visible without horizontal scrolling
4. Verify: On narrow screens the title appears above the buttons (stacked), not side by side
5. Scroll to the bottom of the event list — confirm the last card is not hidden behind the bottom navigation bar
6. Switch back to desktop viewport — confirm both buttons are side by side and layout is correct

**Test 2 — PDF import checkboxes:**
1. Navigate to /coach/events/import-pdf
2. Upload a test PDF
3. Advance to step 2 (team selection)
4. After selecting a team name from the PDF dropdown, verify:
   - The match table has a checkbox column
   - A "select all" checkbox is in the table header
   - Each row has its own checkbox, all pre-checked by default
   - Deselecting individual rows updates the count in the subtitle
   - Deselecting all rows disables the "Weiter" button
5. Reselect some rows and proceed — verify only selected count appears in step 4

**Test 3 — Duplicate detection:**
1. Import a PDF containing matches for a team that already has events on those dates
2. In step 4, click "Termine erstellen"
3. Verify: A dialog appears saying "[N] Spiele existieren bereits..."
4. Test "Uberspringen" — confirm only non-duplicate events are created

**Test 4 — No debug output:**
1. Open browser DevTools Console
2. Upload a PDF — verify no "=== PDF DEBUG INFO ===" lines appear in the console
  </how-to-verify>
  <verify>
    <automated>MISSING — visual mobile rendering requires human verification</automated>
    <manual>Complete all 4 test scenarios above and confirm pass/fail for each</manual>
  </verify>
  <done>All 4 test scenarios pass: mobile layout correct, checkboxes functional, duplicate dialog works, no debug output in console</done>
  <resume-signal>Type "approved" if all tests pass, or describe any issues found (e.g., "layout ok, duplicate dialog not showing")</resume-signal>
</task>

</tasks>

<verification>
Human confirms all four test scenarios pass.
</verification>

<success_criteria>
- Mobile layout: no overflow, no navigation overlap, buttons accessible on 375px viewport
- PDF import: checkboxes present and functional, selection count updates, Weiter gated
- Duplicate detection: dialog appears, skip/overwrite both work correctly
- No debug logs in console or server output
</success_criteria>

<output>
After approval, create `.planning/phases/02-pdf-import-mobile-layout/02-03-SUMMARY.md` with:
- Verification results from human testing
- Any issues found and resolved
- Phase 2 completion status
</output>
